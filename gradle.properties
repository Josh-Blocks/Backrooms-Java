# Gradle
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# Loom is not yet fully compatible with the configuration cache.
org.gradle.configuration-cache=false

# Fabric — verified against fabricmc.net/develop & Modrinth on 2026-06-24.
# Do not bump these from memory; re-check the live versions first.
minecraft_version=1.21.11
loader_version=0.19.3
loom_version=1.17.12
# Mappings: official Mojang mappings (loom.officialMojangMappings() in build.gradle).
# MC 1.21.11 ships non-obfuscated, so Loom 1.17 cannot layer Yarn here.

# Mod
mod_version=0.1.0
maven_group=com.joshsblocks
archives_base_name=joshsblocks-backrooms

# Dependencies
fabric_version=0.141.4+1.21.11
